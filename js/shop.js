(async()=>{const all=(await FLIPCO.load()).filter(p=>FLIPCO.stock(p)>0),e=FLIPCO.esc,grid=document.querySelector('#productGrid'),count=document.querySelector('#shopCount');let state={cat:FLIPCO.param('category')||'all',brand:FLIPCO.param('brand')||'all'};const card=p=>`<a class="shop-card" href="product.html?id=${encodeURIComponent(p.id)}"><div class="shop-img"><img src="${e(p.image)}" alt="${e(p.brand)} ${e(p.name)}" loading="lazy"><span>${e(p.badge||'SELECTED')}</span></div><div class="shop-meta"><div><small>${e(p.brand)}</small><b>${e(p.name)}</b></div><strong>${FLIPCO.money(p.price)}</strong></div></a>`;function render(){let ps=all;if(state.cat!=='all')ps=ps.filter(p=>p.category===state.cat);if(state.brand!=='all')ps=ps.filter(p=>p.brand===state.brand);grid.innerHTML=ps.map(card).join('')||'<div class="empty-grid">Nessun prodotto nella selezione.</div>';count.textContent=String(ps.length).padStart(2,'0')+' CAPI';document.querySelectorAll('[data-cat]').forEach(b=>b.classList.toggle('active',b.dataset.cat===state.cat));}
document.querySelector('#shopFilters').innerHTML=`<div class="filter-group"><button data-cat="all">TUTTO</button><button data-cat="Uomo">UOMO</button><button data-cat="Donna">DONNA</button><button data-cat="Kids">KIDS</button></div><div class="shop-sort"><span id="shopCount"></span><button id="clearFilter">RESET ↺</button></div>`;document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{state.cat=b.dataset.cat;state.brand='all';render()});document.querySelector('#clearFilter').onclick=()=>{state={cat:'all',brand:'all'};history.replaceState({},'', 'shop.html');render()};render();})();