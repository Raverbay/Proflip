(async()=>{const ps=await FLIPCO.load(),params=new URLSearchParams(location.search);let f=params.get('category')||'all',brand=params.get('brand')||'';const e=FLIPCO.esc;function render(){const a=ps.filter(p=>FLIPCO.stock(p)>0&&(f==='all'||p.category===f)&&(!brand||p.brand===brand));shopCount.textContent=`${a.length} ${a.length===1?'PRODOTTO':'PRODOTTI'}`;productGrid.innerHTML=a.map(p=>`<article class="product-card"><a href="product.html?id=${encodeURIComponent(p.id)}"><div class="product-image"><img src="${e(p.image)}" alt="${e(p.brand)} ${e(p.name)}" loading="lazy"><span class="badge">${e(p.badge||'SELECTED')}</span></div><div class="product-info"><div><small>${e(p.brand)}</small><h2>${e(p.name)}</h2></div><strong>${FLIPCO.money(p.price)}</strong></div></a></article>`).join('')||'<div class="empty">Nessun prodotto disponibile.</div>'}document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{f=b.dataset.filter;brand='';history.replaceState(null,'',`shop.html${f==='all'?'':`?category=${encodeURIComponent(f)}`}`);document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===b));render()});render()})();
