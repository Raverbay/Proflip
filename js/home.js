(async()=>{const ps=(await FLIPCO.load()).filter(p=>FLIPCO.stock(p)>0);if(!ps.length)return;const e=FLIPCO.esc,hero=ps.find(p=>p.badge==='NEW')||ps[0], edits=ps.slice(1,7);const image=p=>`<img src="${e(p.image)}" alt="${e(p.brand)} ${e(p.name)}" loading="lazy">`;
document.querySelector('#homeHero').innerHTML=`<section class="hero-stage"><div class="hero-ghost">FLIP&CO</div><div class="hero-side"><div class="eyebrow">01 / THE CURRENT EDIT</div><div class="hero-title"><span>SELECTED</span><em>IN</em><span>CAGLIARI</span></div><div class="hero-side-bottom"><p>Brand internazionali.<br>Una selezione indipendente.</p><a class="arrow" href="shop.html">ENTER SHOP ↗</a></div></div><a class="hero-image" href="product.html?id=${encodeURIComponent(hero.id)}">${image(hero)}<div class="hero-frame"></div><div class="hero-product"><small>${e(hero.brand)} · ${e(hero.category)}</small><h1>${e(hero.name)}</h1><span>SCOPRI IL CAPO ↗</span></div><b class="hero-counter">01 / ${String(Math.min(12,ps.length)).padStart(2,'0')}</b></a><div class="hero-stamp">VIA ITALIA 22<br>09124 CAGLIARI<br><span>SINCE 1986</span></div></section>`;
document.querySelector('#editGrid').innerHTML=edits.slice(0,2).map((p,i)=>`<a class="edit-card ${i?'offset':''}" href="product.html?id=${encodeURIComponent(p.id)}"><div class="edit-img">${image(p)}</div><div class="edit-meta"><small>0${i+2} / ${e(p.brand)}</small><b>${e(p.name)}</b><span>${FLIPCO.money(p.price)} · SCOPRI ↗</span></div></a>`).join('');
document.querySelector('#rail').innerHTML=edits.map((p,i)=>`<a class="rail-card" href="product.html?id=${encodeURIComponent(p.id)}"><div class="rail-no">${String(i+2).padStart(2,'0')}</div><div class="rail-img">${image(p)}</div><div class="rail-info"><small>${e(p.brand)}</small><b>${e(p.name)}</b></div></a>`).join('');
document.querySelector('#brandList').innerHTML=[...new Set(ps.map(p=>p.brand))].sort().map((b,i)=>`<a href="shop.html?brand=${encodeURIComponent(b)}"><span>${String(i+1).padStart(2,'0')}</span><b>${e(b)}</b><i>↗</i></a>`).join('');
document.querySelector('#heroMeta').textContent=`${String(Math.min(12,ps.length)).padStart(2,'0')} ITEMS IN THE CURRENT EDIT`;
})();