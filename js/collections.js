(async()=>{const [cs,ps]=await Promise.all([FLIPCO.collections(),FLIPCO.load()]),e=FLIPCO.esc;const by=c=>ps.filter(p=>FLIPCO.stock(p)>0&&((c.filter.badge&&p.badge===c.filter.badge)||(c.filter.category&&p.category===c.filter.category)||(c.filter.type&&p.type===c.filter.type)));collectionsList.innerHTML=cs.map((c,i)=>{const a=by(c),p=a[0]||ps.find(x=>FLIPCO.stock(x)>0);return `<a class="collection-card" href="collection.html?slug=${encodeURIComponent(c.slug)}"><div class="collection-card-image">${p?`<img src="${e(p.image)}" alt="${e(c.title)}" loading="lazy">`:''}</div><div><span>${e(c.eyebrow)}</span><strong>${e(c.title)}</strong><i>${a.length} CAPI ↗</i></div></a>`}).join('')})();
