(async()=>{const ps=(await FLIPCO.load()).filter(p=>FLIPCO.stock(p)>0),cs=FLIPCO.DATA.collections,e=FLIPCO.esc;document.querySelector('#collectionsList').innerHTML=cs.map((c,i)=>{let p=ps.find(x=>c.filter?.category?x.category===c.filter.category:c.filter?.badge?x.badge===c.filter.badge:c.filter?.type?x.type===c.filter.type:true))||ps[i%ps.length];return `<a class="collection-row" href="collection.html?slug=${encodeURIComponent(c.slug)}"><div class="collection-copy"><small>${e(c.eyebrow)}</small><h2>${e(c.title)}</h2><p>${e(c.description)}</p><span>EXPLORE ↗</span></div><div class="collection-image"><img src="${e(p?.image||'assets/store-entrance.jpg')}" alt="${e(c.title)}" loading="lazy"></div></a>`}).join('')})();